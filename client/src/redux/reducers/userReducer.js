import {} from "./../actionCreators";

const initialState = {
  userLoggedIn: false,
  authJWT: undefined
};

const user = (state = initialState, action) => {
  return state;
};

export default user;
