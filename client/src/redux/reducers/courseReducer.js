import {} from "../actionCreators";

const initialState = {};

const course = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default course;
